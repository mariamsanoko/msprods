{
  "name": "msprods-airtable-brain",
  "version": "1.0.0",
  "description": "MS Prods Airtable Brain chatbot powered by Airtable and OpenAI Responses API.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/airtable.js && node --check src/chat-service.js && node --check src/recommendation-engine.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  }
}
